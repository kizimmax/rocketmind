import { LensDemoClient } from "./LensDemoClient";

export const metadata = {
  title: "RoundGlassLens | Rocketmind Design System",
  description: "WebGL glass lens effect component — optical refraction, chromatic dispersion, parallax.",
};

export default function LensDemoPage() {
  return <LensDemoClient />;
}
