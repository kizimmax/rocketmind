-- «Программа активна» — свитч, по которому суперадмин закрывает бесплатный
-- доступ участников. После выключения участники получают модалку «программа
-- завершена» и read-only-чат с плашкой подписки.
ALTER TABLE "programs"
  ADD COLUMN "isActive" BOOLEAN NOT NULL DEFAULT TRUE;
